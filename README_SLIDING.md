# Sliding Window Problems (1-8)

File: `sliding_window.py`

Contains implementations for the requested sliding-window tasks and quick tests.

Run tests:

```bash
python -m sliding_window
```

To push to GitHub:

```bash
git remote add origin <URL>
git branch -M main
git push -u origin main
```

Or use GitHub CLI if available:

```bash
gh repo create <owner>/<repo> --public --source=. --remote=origin --push
```
